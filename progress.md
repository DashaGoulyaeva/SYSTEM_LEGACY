Original prompt: Пользователь просит помогать пилить SYSTEM_LEGACY, читать и учитывать проектные заметки и правила из Obsidian, самостоятельно писать код, вести заметки о разработке, не делать коммиты/пуши, а после каждого изменения давать короткий и контекстный сценарий ручной проверки.

## 2026-03-22

- Первый слой доведён до более ясного цикла с очередью ошибок, отдельными действиями `Диагностика` и `Исправить`, повышенной частотой инцидентов и целью `20` разборов до дефрагментации.
- Сегодняшняя добивка:
  - память откатил от слишком жёсткого дефицита к среднему режиму;
  - формат памяти упростил до человекочитаемого `7 КБ из 32 КБ`;
  - убрал лишнюю фразу `Ошибка может сформироваться без вмешательства.`;
  - сообщения очереди получил отдельный визуальный вариант в журнале.
- Зафиксирован отложенный техэтап: полноценный CRT-визуал делаем после завершения базовой логики всей игры, ориентир — `Three.js + postprocessing`, не сейчас.
- Для будущих проверок добавлены рабочие hooks `window.render_game_to_text` и `window.advanceTime`, чтобы дальше было проще гонять состояние игры без угадываний.
- Начат каркас второго слоя: при переходе на `РЕГЛАМЕНТ` появляется отдельный слойный вход в журнал, сохранённый как часть состояния, чтобы потом наращивать его собственные правила без вмешательства в первый слой.
- Для автоматизации и стабильных прогонов: добавлен `automation mode` через `?automation=1` (отключает фоновый `setInterval(gameTick, TICK_MS)`; для шагов использовать `window.advanceTime`).
- Для сохранений: добавлены поля `saveVersion/schemaVersion=1` в payload и мягкая обработка старых сохранений без версии при загрузке.
- `window.advanceTime(0)` теперь ведёт себя как no-op, чтобы детерминированные проверки не съедали лишний тик на нулевом шаге.
- Уточнены служебные сообщения очереди и действий: понятнее, что делает ДИАГНОСТИКА и ИСПРАВИТЬ, и когда очередь очищена.

### Браузерный smoke

- Smoke на `?automation=1` прошел: детерминированный шаг времени, save/load roundtrip, восстановление смены после reload и отсутствие console errors.
- Дополнительно прогнан Playwright-контур после правок текстов (команды/очередь). Ошибок в консоли не зафиксировано.
- Выровнял терминологию: в интерфейсе и сводках теперь `ошибка`, а не `сбой`, когда речь об одной и той же пользовательской сущности.
- Следующий полезный шаг: вторичная проверка `Исправить` на живой очереди, уже без изменения базовой вертикалки.
- Проверка `Исправить` пройдена: очередь 1 -> 0, `advanceTime(0)` no-op, save/load остаётся корректным, ошибок в консоли нет.

## TODO

- Проверить с пользователем, попал ли новый баланс памяти в нужную середину между "слишком много" и "слишком мало".
- Если очередь всё ещё читается недостаточно ясно, вынести число ошибок в ещё более заметный формат внутри строки журнала.
- Когда дойдём до общего техэтапа графона, составить подробный план миграции на `Three.js + postprocessing` с объяснением, что именно ставить и зачем.
